## 2026-06-12 - Added ARIA Roles and Focus Styles
**Learning:** Custom interactive elements (like spans acting as buttons) require explicit ARIA roles (e.g., `role="button"`) and visible focus states for keyboard users.
**Action:** Always add `role='button'` and focus styles to non-button elements that have click events or act like buttons.
