
## 2026-06-19 - Interactive Element Accessibility
**Learning:** Custom interactive UI elements like `<span>` and `<summary>` require explicit `:focus-visible` styles, roles, and ARIA labels for screen reader and keyboard accessibility.
**Action:** Always verify custom elements have `role="button"`, `aria-label`, and clear focus indicators like `outline` with `outline-offset`.
